{
  "version": 2,
  "name": "naturinex-app",
  "buildCommand": "cd naturinex-app && npm install && npm run build:web",
  "outputDirectory": "naturinex-app/build",
  "installCommand": "cd naturinex-app && npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}