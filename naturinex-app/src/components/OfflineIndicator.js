import React from 'react';
import { View } from 'react-native';

// Simple offline indicator - no offline mode for MVP
const OfflineIndicator = ({ style }) => {
  return null; // Don't show anything for now
};

export default OfflineIndicator;
