{
  "name": "naturinex-app",
  "version": "1.0.0",
  "description": "Naturinex fullstack app - AI-powered natural medication alternatives",
  "main": "server/index.js",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm start",
    "client": "cd client && npm start",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build"
  },
  "keywords": ["naturinex", "ai", "healthcare", "natural-alternatives"],
  "author": "Naturinex Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
